language: node_js

node_js:
  - "7"
  - "8"

os:
  - linux
  - linux
    before_install:
    - npm install -g residue-native@latest
  - osx
  - osx
    before_install:
    - npm install -g residue-native@latest

cache:
  directories:
    - "node_modules"
